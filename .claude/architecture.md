# Architecture

Cargo workspace with five crates:

- **`shared/`** (`omnibus-shared`) — serde types shared across every target (`Settings`, `ValueResponse`, `LibraryContents`, `LibrarySection`). No Dioxus / axum / sqlx deps.
- **`db/`** (`omnibus-db`) — server-side data layer: SQL migrations, SQLite pool init, the normalized query layer, and the indexing pipeline (scanner → ebook metadata extraction → atomic per-library upsert). Consumed by both `server/` (REST handlers) and `frontend/` (server-function bodies). Holds all sqlx / tokio / epub / anyhow dependencies on the server side.
- **`frontend/`** (`omnibus-frontend`) — Dioxus UI + server-function wire layer (`rpc/`, split into per-domain submodules re-exported from `rpc/mod.rs`). Feature-gated:
  - `web` — WASM client build (used by `server/` when `dx serve --platform web` builds it for WASM).
  - `mobile` — Native Dioxus build; uses `reqwest` against `/api/*` REST routes.
  - `server` — SSR/native build; pulls in `omnibus-db` and compiles server-function bodies. Name is hardcoded by the dioxus_fullstack_macro — can't be renamed.
- **`server/`** (`omnibus`) — **unified Dioxus fullstack binary**. Built twice by `dx serve`: once native (feature `server`) for the axum backend + SSR, once WASM (feature `web`) for the hydrated client. Hosts the hand-written `/api/*` REST router for mobile. Depends directly on `omnibus-db`.
- **`mobile/`** (`omnibus-mobile`) — thin Dioxus Native shell (~16 lines) that injects `ServerUrl` context and launches `omnibus_frontend::App`.

Default `cargo build` / `clippy` covers `server`, `shared`, `frontend` only. Mobile is excluded via workspace `default-members` because its `mobile` feature is mutually exclusive with `web`; build it explicitly: `cargo build -p omnibus-mobile`.

**Web request flow (fullstack):** browser → axum serves SSR'd HTML + WASM bundle → hydration → signal effects call Dioxus server functions (`#[get]`/`#[post]` in `frontend/src/rpc/`) at `/api/rpc/*` → same handlers execute server-side against the SQLite pool via an `axum::Extension<SqlitePool>` layer.

**Mobile data flow:** Dioxus signal/effect → `reqwest` call to `/api/*` (hand-written handlers in `server/src/backend.rs`) → signal update → re-render. Mobile deliberately does **not** use the `/api/rpc/*` server functions.

**Database:** schema ships as numbered SQL migrations under [db/migrations/](../db/migrations/), embedded via `sqlx::migrate!` and run on pool init in `omnibus_db::init_db`. Applied versions are recorded in the `_sqlx_migrations` table. Add new migrations as `NNNN_description.sql` — never edit an applied file. All tests use `sqlite::memory:` for isolation; the migrator runs against them the same as production.

**Server URL (mobile):** hardcoded to `http://127.0.0.1:3000` in `mobile/src/main.rs` via `use_context_provider`. Will become a first-launch setup screen.

## shared/src/

Domain types are split by sub-topic and re-exported flat from `lib.rs`, so callers keep using `omnibus_shared::Foo` regardless of which submodule `Foo` lives in.

```
lib.rs              — thin module index: pub mod / pub use audiobook::*, auth::*, discovery::*, ebook::*, highlight::*, journal::*, progress::*, ratings::*, settings::*, shelves::*, view_prefs::*, worker::* + image_format::detect_image_format
image_format.rs     — magic-byte image-format sniff (detect_image_format); pure &[u8] inspection, single source of truth for both server::backend and frontend::rpc upload paths
upload.rs           — "add your own books" wire types (UploadInspection, UploadCommitResult) + detect_ebook_format magic-byte sniff (EPUB = ZIP); shared by backend::uploads and frontend data::uploads
settings.rs         — Settings, LibrarySection, LibraryContents (library config + GET /api/library payload)
ebook/              — Contributor, Identifier, EbookMetadata (metadata.rs) + MetadataOverrides w/ validate + merge (overrides.rs); tests.rs sibling
merge.rs            — MergeBooksResult (F5.10 /api/rpc/merge-books wire shape)
discovery.rs        — EbookLibrary, AuthorDetail, AuthorPhotoScanResult, SeriesDetail, TagWeight, AuthorSummary, SeriesSummary + PaletteResults / Palette*Hit (F1.5 grouped command-palette results)
progress.rs         — F2.1 ProgressFormat, ProgressUpdate (+validate), ProgressRecord, SessionReport (+validate); tests.rs sibling
highlight.rs        — F2.4b HighlightColor, Highlight, CreateHighlight, UpdateHighlightNote (highlight annotation wire types)
ratings.rs          — F3.2 RatingUpdate (+validate, 0.5–5.0 half-star steps, +half_stars() storage form), RatingRecord (stars: f32), stars_from_half_stars helper (per-user star-rating wire types); tests.rs sibling
journal.rs          — F3.2 public journal wire types: JournalEntry (id/book_uuid/author_id/author_name/body_md/body_html/progress/created_at/updated_at), CreateJournalEntry + UpdateJournalEntry (+validate: non-empty, BODY_MAX_LEN 64 KiB, progress ≤ 100); tests.rs sibling
shelves.rs          — F3.1 shelf wire types: ShelfKind/Visibility/MatchMode/RuleField/RuleOp enums (as_str/from_str), ShelfRule (+validate via RuleField::accepts), ShelfSummary (rail row), Shelf (detail), CreateShelfRequest/UpdateShelfRequest (+validate kind invariants + name len), RulePreview/RulePreviewRequest, ShelfPage. Condition shape shared with F1.5 advanced search. Note: RuleField::Status is defined but unsupported in v1 (no read-completion signal in schema). shelves/tests.rs sibling
audiobook.rs        — F2.3 ManifestPart, ChapterInfo, AudiobookManifest::{Direct(+chapters),Hls} (GET /api/audiobooks/:uuid/manifest payload)
view_prefs.rs       — F1.3 ViewMode, SortKey, SortDir, ViewFilters, ViewPrefs (persisted landing-page sort/filter state)
auth.rs             — UserSummary, LoginRequest, RegisterRequest, LoginResponse (LoginRequest/RegisterRequest deliberately do not derive Debug — they carry plaintext passwords)
worker.rs           — TaskKind, ProgressState (Running/Done/Failed), TaskProgress, WorkerStatus (worker progress feed wire shape)
```

## db/src/

```
lib.rs              — module index + flat re-export of the query layer (books, browse, covers, discovery, helpers, identity, merge, metadata_overrides, missing_files, palette, pool, settings, sync, author_photos_data, thumbs) so callers write `omnibus_db::list_books(...)`. Declares 29 `pub mod`s plus a private `taxonomy` and a cfg-gated `test_support`.
books/              — core book read layer: `list_books`/`list_books_for_paths`, `get_book`/`get_book_by_uuid`, `search_books` (BM25 FTS), the shared `BOOK_COLUMNS` projection, `book_file_path`, and `resolve_book_id_by_uuid` (UNIONs `merged_uuids` so format-merged uuids still resolve). `MAX_BOOKS_RETURNED` cap; `BooksError`. F5b keyset landing path lives in `page` (`list_books_page` — opaque `PageCursor` over any of the 5 sort axes, server-side `ViewFilters`, NULL-aware keyset matching the `(library_id,<col>,id)` indexes from 0028; `CursorError`) and `facets` (`library_facets` — full-library author/series/format/tag count aggregate for the sidebar). Split into submodules (`list`/`get`/`search`/`projection`/`page`/`facets`) + `tests.rs`.
browse.rs           — F1.12 browse-all indexes: `list_authors`/`list_series` with per-row `book_count`, accent color, and `has_photo`, honoring metadata overrides.
covers.rs           — on-disk cover blob access: `covers_dir`, `get_cover`, `get_last_modified_epoch`; `CoversError`. Cover image files live on the filesystem, not in the DB.
discovery/          — F1.8 discovery-detail reads: one author or series with their books, plus the global tag cloud. DB-wide reads (no per-user ACL yet). Split into `discovery/{mod,authors,series,tags}.rs` + `tests.rs`.
helpers.rs          — shared query helpers: `mint_uuid` (random UUIDv4 — the durable book identity, F2: assigned once at insert, never recomputed), `scan_key_for` (the library-relative path = the Phase-A diff key), `stable_uuid` (UUIDv5 over `{library_path}\0{filename}` — retained only as the `merged_uuids` cross-format attach-ledger key, no longer book identity), FTS query building (`build_fts_match`, `sanitize_fts_query`).
identity.rs         — F2 boot backfill `backfill_scan_keys`: fills `books.scan_key` + `merged_uuids.scan_key` for pre-migration (0026) rows purely from stored columns (no filesystem reads), idempotent, run from `init_db` after `backfill_norm_columns`.
metadata_overrides/ — user metadata edits keyed by `book_uuid TEXT` (no FK — survives the indexer's DELETE/INSERT reindex cycle): CRUD + shallow-merge into book reads + an FTS-rebuild helper (`rebuild_fts_for_book(_batch)`) that resolves uuids via `resolve_book_id_by_uuid` (merged-uuids fallback), writes the canonical row through the `sync::fts` door, then overlays override text. Split into submodules + `tests.rs`.
missing_files.rs    — F10 missing-files GC: `gc_books_missing_files` hard-deletes `books` rows fileless past a 30-day retention (`MISSING_FILES_RETENTION_DAYS`) that carry no user data (0027 soft-ref tables + F3.2 `user_ratings` + F3.2 `journal_entries`), no `merged_uuids` attachment, and no wishlist override, taking their `metadata_overrides` row, `books_fts` twin, cover files, and any now-orphan taxonomy (`taxonomy::delete_orphan_taxonomy`) along; `backfill_missing_files_flags` stamps pre-0029 fileless rows at boot. Flags (`is_missing_files`/`missing_files_since`/`is_missing_files_override`) are set by `sync::mark_book_files_missing` (which drops only `book_files`, so a fileless book keeps its links/FTS and stays in author/series/tag browse + search — only the grid/facets hide it) and cleared at the file-write chokepoints. Best-effort call after each reindex. `MissingFilesError`; `tests.rs` sibling.
palette/            — F1.5 command-palette search: grouped FTS5 results across books/authors/series/tags (`search_palette`). Split into submodules + `tests.rs`.
pool.rs             — SQLite pool init: `init_db(url)` runs the embedded `sqlx::migrate!` set, then the idempotent boot backfills (`normalize::backfill_norm_columns`, `identity::backfill_scan_keys`, `sort_keys::backfill_series_sort`, `missing_files::backfill_missing_files_flags`). Canonical initializer for production and `sqlite::memory:` tests.
settings.rs         — settings KV store (library paths, etc.); changing a configured path **repoints** the existing `scan_roots` row in place (F2 — keeps `library_id`/`books.uuid` stable across the move), and `prune_orphan_libraries` is never-prune: it drops only *childless* orphan roots, never cascade-deleting a root's books. `SettingsError`.
taxonomy.rs         — (private `mod`) internal authors/series/tags link upserts shared by the `sync` write path.
test_support.rs     — `#[cfg(any(test, feature = "test-support"))]` shared test fixtures: in-memory pool init, seed factories, `EnvVarGuard`, `CoversTempDir`, `indexed_audiobook`. Behind the `test-support` feature for cross-crate reuse.
progress.rs         — F2.1 server-authoritative reading/listening position + batched session reports. `upsert_progress` (last-write-wins on `(user, book_uuid, format)`) / `get_progress` / `record_session`; all soft-reference the durable `books.uuid` (F1 — `book_uuid TEXT`, no FK, no cascade, so a removed/reindexed book detaches rather than cascade-deleting user data) via `resolve_canonical_book_uuid` (folds `merged_uuids`). Backs `/api/progress*` REST (mobile) and `/api/rpc/progress*` server functions (web). Schema migrations/0013_reading_progress.sql + 0027 (soft-ref recreate) lay out `bookmarks` / `reading_sessions` / `listening_sessions`.
highlights/         — F2.4b highlight annotation CRUD (`create_highlight`, `list_highlights`, `update_highlight_color`, `update_highlight_note`, `delete_highlight`); soft-references the durable `books.uuid` (F1 — `book_uuid TEXT`, no FK/cascade; recreated by migration 0027). Stores the highlighted prose in a `text` column (migration 0030, nullable) so drawers + quote cards render the passage. `HighlightError` thiserror enum (BookNotFound/NotFound/Sqlx). Backs `/api/highlights*` REST (mobile) and `/api/rpc/highlights/*` server functions (web). Schema: migrations/0017_highlights.sql. tests.rs sibling.
bookmarks/          — F2.3b/F2.4b unified bookmark CRUD (`create_bookmark`, `list_bookmarks`, `update_bookmark`, `delete_bookmark`); one model serves the audiobook player (`position` = seconds-as-string) and the reader (`position` = EPUB CFI). Soft-references `books.uuid` via `resolve_canonical_book_uuid`; `BookmarkError` thiserror enum (BookNotFound/NotFound/Sqlx). Backs `/api/bookmarks*` REST (mobile) and `/api/rpc/bookmarks/*` server functions (web). Reuses the `bookmarks` table from migration 0013/0027 (no new migration). tests.rs sibling.
ratings/            — F3.2 per-user star ratings (`set_rating` upsert last-write-wins on `(user, book_uuid)`, `get_rating`, `delete_rating`/un-rate); stored as `half_stars` (1–10 = 0.5–5.0★) so the UI offers half-star granularity. Soft-references the durable `books.uuid` (no FK/cascade) via `resolve_canonical_book_uuid` (folds `merged_uuids`). `RatingError` thiserror enum (BookNotFound/Sqlx). Backs `/api/ratings*` REST (mobile) and `/api/rpc/ratings/*` server functions (web). Schema: migrations/0031_user_ratings.sql. tests.rs sibling.
shelves/            — F3.1 shelves (smart + hand-picked library subsets): `mod.rs` (ShelfError, can_view/can_edit visibility helpers), `rules.rs` (smart rule→SQL `membership_predicate`: EXISTS subqueries over normalized tags/authors/series/format/year + owner-scoped `user_ratings`; date fields over `books.timestamp`/`last_modified` with in-last/between/before/after; `status` rejected — no schema signal), `read.rs` (`list_visible_shelves` owner∨public∨admin with live counts, `get_shelf`, `shelf_page` capped, `preview_rule`), `write.rs` (`create_shelf` name-unique-per-owner→`NameTaken`, `update_shelf`, `delete_shelf`, `add_books`/`remove_book` uuid-soft-ref via `resolve_canonical_book_uuid`, auto accent). Backs `/api/shelves*` REST (mobile) + `/api/rpc/shelves*` server functions (web). Schema: migrations/0034_shelves.sql. tests.rs sibling.
journals/           — F3.2 **public** per-book journal entries (`create_journal_entry`, `list_journal_entries` returns *every* user's entries newest-first joined to `users` for `author_name`, owner-scoped `update_journal_entry`/`delete_journal_entry` → `NotFound` for a non-owner). Soft-references the durable `books.uuid` (no FK/cascade) via `resolve_canonical_book_uuid`. `markdown.rs` renders the body server-side (pulldown-cmark + ammonia, with a `||spoiler||` → `<button class="spoiler" type="button" aria-expanded="false">` pass — a native button so keyboard/screen-reader users can reveal it too) on read. `JournalError` thiserror enum (BookNotFound/NotFound/Sqlx). Backs `/api/journals*` REST (mobile) and `/api/rpc/journals/*` server functions (web). Schema: migrations/0032_journal_entries.sql. markdown.rs + tests.rs siblings.
suggestions/        — F3.3 "Readers also enjoyed" (Hardcover community-list co-occurrence) split into four submodules: `hardcover.rs` (GraphQL client against `HardcoverConfig` — `resolve_book` ISBN-first then exact-title fallback, `curated_list_ids`, `co_listed_counts` ranking, `fetch_candidates`; `HardcoverError`, wiremock-injectable base URL), `filter.rs` (pure `filter_candidates`/`is_entry_point`/`is_same_author`/`is_same_series` trimming to different-author/-series entry-point survivors — no I/O), `data.rs` (cache CRUD over migration 0033's `book_suggestion_state`/`book_suggestions` tables + the pure `decide` state machine returning `CacheDecision{serve,enqueue}` from a `(state,fetched_at)` marker, `SUGGESTIONS_TTL_SECS` 30-day TTL + `PENDING_DEBOUNCE_SECS` so a burst of first-viewers collapses to one job; `suggestion_state`/`mark_pending`/`replace_suggestions`/`get_suggestions`/`get_suggestion_cover`/`delete_suggestions`; `SuggestionsDataError`), and `cascade.rs` (`resolve`/`resolve_with` orchestration driven by `worker::Task::ResolveSuggestions` — resolve → list → rank → filter → best-effort cover fetch → cache top 10; sticky `empty` on a clean miss, marker untouched on a transient error so the debounce re-posts). Reads the effective Hardcover key (settings → env). tests.rs sibling.
auth.rs             — F0.3 auth data layer (users/devices/sessions): Argon2id hash + verify with PHC rotation, password-policy validation, race-free first-user-admin (BEGIN IMMEDIATE), per-account lockout, SHA-256-hashed session tokens with absolute + idle expiry, `promote_to_admin` recovery hook. Pure SQL + hashing — cookies/axum live in `server/src/auth/`. `validate_session(pool, authorization, cookie_header) -> Result<(User,Session), SessionAuthError>` is the single consolidated cookie/bearer → live-session surface (axum-free, pure-string headers) that both HTTP auth extractors (`server::auth::extractor` + `omnibus_frontend::rpc`) delegate to so they can't drift. Schema: migrations/0004_auth.sql
scanner.rs          — library directory scanning
ebook.rs            — EPUB OPF metadata + cover extraction; sidecar-first cover with opt-in materialization (ScanOptions)
audiobook.rs        — F2.3 audiobook metadata extraction via `lofty` (m4b/m4a/mp3): title/artist/album/duration + embedded artwork. Phase A.5 `group_into_books` groups per-file stat entries into one `AudiobookGroup` per book (folder-of-mp3s = one group; same-dir m4b/m4a files whose stems match after `strip_part_suffix` — Pt1/Part 2/Disc 3/1 of 3/- 2 — also group, keyed on the first part's path so a lone Pt1's uuid survives Pt2's arrival). Phase B `parse_groups` reads ID3 tags for every part, sorts by (track, part number, filename), extracts each part's chapters onto one continuous offset timeline, and emits `IndexedAudiobook` rows for `sync::sync_audiobooks`. Submodule `audiobook::codec` (#339) hosts `PlaybackMode`, `classify_filenames`, `mime_for_filename` — the gate that decides direct-play vs HLS for the new manifest endpoint. Submodule `audiobook::chapters` extracts Nero `chpl` atoms (M4B/M4A) and ID3v2 CHAP frames (MP3) into `RawChapter` structs; called during Phase B parse.
hls.rs              — F2.3 HLS transcode cache (fallback path after #339): `resolve_audiobook` (only admits books whose `book_files.format` is M4B/M4A/MP3), `get_parts`, `get_chapters` (file_chapters query for manifest), `build_manifest` (VOD m3u8 from stored durations), `transcode_book` (ffmpeg concat→AAC-LC 64 kbps mono, 10 s segments), `evict_if_over_cap` (FIFO-by-mtime). Only invoked for non-natively-playable parts (a flac mixed into an mp3 folder, etc.); m4b/m4a/mp3 direct-play via `/api/audiobooks/:uuid/parts/:ordinal`. Reads `OMNIBUS_DATA_DIR`, `OMNIBUS_HLS_CAP_BYTES`, `OMNIBUS_FFMPEG_PATH`, `OMNIBUS_HLS_TRANSCODE_TIMEOUT_SECS` — see `.env.example` for defaults.
thumbs.rs           — F1.2 thumbnail pipeline: decode cover → resize to Sm/Md/Lg (2:3) → atomic per-(book,size) WebP writes under OMNIBUS_THUMBS_DIR; FIFO-by-mtime eviction once total cache exceeds OMNIBUS_THUMBS_CAP_BYTES (default 5 GiB). Backs backend.rs's cover-to-WebP serving path
indexer.rs          — scan → DB indexing, staleness checks (is_stale + reindex); reindex opts into materialize_sidecars. `diff_library` matches disk-vs-DB on the F2 `scan_key` (library-relative path) so a scan-root repoint preserves every `books.uuid`; the Removed bucket **ghosts** (drops `book_files`, retains the `books` row, flags it missing for F10 GC), and a fileless ghost whose file reappears routes through Changed to re-attach (clearing the flag). After each reindex a best-effort `missing_files::gc_books_missing_files` purges long-missing, user-data-free ghost rows. F2.3 `reindex_audiobooks` uses Phase A.5 group_into_books + Phase B parse_groups + sync_audiobooks (writes book_file_parts rows). Both reindex paths extend the diff input with `list_merged_rows_for_formats` (merged_uuids ⋈ book_files) so files attached to another book classify Unchanged/Changed instead of resurrecting duplicates.
normalize.rs        — title/author normalization for the cross-format auto-attach match key (`books.title_norm`/`author_norm`): deunicode fold + casefold + punctuation collapse + "Last, First" swap; exact-match only (Dune ≠ Dune Messiah). `backfill_norm_columns` runs once at boot from `init_db`.
sort_keys.rs        — F5b denormalized sort-key columns on `books`: `series_sort_value` (write-time primary-series-name pick, bound by the `sync` writers + merge undo) and `backfill_series_sort` (boot backfill from the series link for pre-0028 rows; only touches linked-but-NULL rows so it's a true no-op once caught up). Scanned metadata only — overrides ignored, like `normalize`. `SortKeysError`.
sync/               — transactional indexer write path: `books.rs` (sync_books buckets), `audiobooks.rs` (sync_audiobooks + parts/chapters), `authors.rs`, `backfill.rs`, `fts.rs` — the single `books_fts` choke-point (`upsert_fts`/`delete_fts` source the row from canonical `books`+links; `rebuild_all_fts` repairs the whole index) every book mutation routes through so the standalone FTS twin can't drift, and `attach/` — cross-format auto-attach (New-bucket title+author match or merged_uuids hit hangs a new file's book_files row off an existing book in another format; Changed refreshes it, Removed drops it leaving the book — possibly fileless, a legal state). The attach path now refreshes the target's FTS row through the door so an attached format's identifiers (incl. ISBN) become searchable immediately.
merge/              — F5.10 manual merge: `merge_books` (one-transaction move of book_files/links/identifiers + progress/sessions/bookmarks/**highlights** re-parented by `book_uuid` with latest-wins progress dedupe + overrides shallow-merge + cover adoption + merged_uuids guard, carrying the source's `scan_key`) and `undo_merge` (recreates the source from the merge_log JSON snapshot, links restored by name). Exposed via /api/rpc/merge-books{,/undo}.
library_layout.rs   — F0.6 canonical layout: slugify, canonical_path, sidecar_cover_for, allocate_canonical_path (collision suffix)
worker/             — single-process Worker primitive: scan_sem (Scan/ScanAudiobooks) + hls_sem (HlsTranscode) semaphores + per-resource keyed mutex; Task::HlsTranscode dispatches into hls::transcode_book; WorkerConfig::hls_concurrency defaults to max(1, num_cpus/2). F1.11 ResolveAuthorPhoto dispatches into author_photos::resolve. Task::RebuildFtsIndex (admin "rebuild search index", keyed "rebuild-fts", no scan sem) dispatches into sync::rebuild_all_fts; exposed via POST /api/fts/rebuild. Split into types (Task/Worker/config), queue (post/await_completion + RAII map-cleanup guards), exec (run dispatch loop), handlers (per-task-kind execute arms), progress (snapshot + retention + report_progress), tests
author_photos/      — F1.11 author profile photo pipeline split into focused submodules: `cascade.rs` (manual → Open Library → letter resolution; `OpenLibraryConfig` for wiremock injection), `remote.rs` (`fetch_remote_image` + SSRF guard — `RemoteImageConfig`, `FetchRemoteImageError`; resolves host, blocks loopback/RFC1918/link-local/AWS IMDS/IPv6 ULA before any TCP connect, pins `reqwest` to validated addresses against DNS rebinding; `allow_private_addresses` is test-only), `shared.rs` (process-wide `reqwest::Client` singleton + `default_user_agent`), `tests.rs` (wiremock-backed cascade + SSRF guard unit tests). Public API re-exported from `mod.rs`.
author_photos_data.rs — Row CRUD for the `author_photos` table (`get_author_photo`, `author_photo_status`, `upsert_author_photo`, `delete_author_photo`) plus `delete_author` admin primitive. `AuthorPhotoSource` enum. `AuthorPhotosDataError` wraps `sqlx::Error` per boundary rule.
migrations/         — numbered SQL migrations embedded via sqlx::migrate!
```

## frontend/src/

```
lib.rs              — Route, App, styles, ScreenLayout (feature-gated); also owns the App-wide `CurrentUser` context (`use_current_user`) — a single `/api/auth/me` fetch on boot fills it, components gate `is_admin` off the cached signal instead of refetching per mount, and a `web_auth_state` subscription refills on login / clears on 401. Web/SSR only — mobile uses bearer tokens. F2.3b: App also provides the app-wide `PlaybackState` context (`use_playback`, defined in contexts.rs alongside CurrentUser), mounts the single persistent `<audio>` element (`pages::AudioElement`) as a sibling of the Router so playback survives navigation, and on web calls `pages::install_audio_bootstrap` — the driver that reacts to `PlaybackState.uuid` (load / swap / dismiss). `ScreenLayout` (web) renders `pages::MiniDock`, so the dock appears on every main page and is naturally absent on the immersive `/listen` + `/read` routes (rendered without `ScreenLayout`).
data.rs             — Module root for the feature-gated data transport (mobile=reqwest, web/server=rpc); owns the `DataError` thiserror enum (Network/Http/Decode/Unauthorized/Other), the mobile transport helpers (`ServerUrl`, `http_client`, `with_bearer`, `note_status`, `drain_error`, `client_kind`), `token_store` (mobile bearer-token store), `web_auth_state` (web 401 channel), and `note_server_fn_err` (web server-fn 401 mapper). Per-domain wrappers live under data/ and are re-exported here.
data/               — Per-domain wrappers split out from data.rs: `books.rs` (settings/library/ebooks/search/overrides/worker), `authors.rs` (author CRUD + photo upload/scan/delete), `series.rs`, `tags.rs`, `progress.rs` (F2.1 reading/listening sync), `highlights.rs` (F2.4b highlight annotation CRUD), `bookmarks.rs` (F2.3b/F2.4b unified bookmark CRUD), `ratings.rs` (F3.2 star-rating set/get/clear), `journals.rs` (F3.2 public journal create/list/update/delete + markdown preview), `suggestions.rs` (F3.3 "readers also enjoyed" fetch + admin Hardcover-key get/set), `shelves.rs` (F3.1 rail list / detail / create / update / delete / member page / add+remove books / rule preview), `auth.rs` (login/register/logout/me + mobile bearer flow), `uploads.rs` (add-books inspect_ebook + upload_ebook + EbookUploadMeta — multipart via gloo-net FormData on web, reqwest on mobile, stub otherwise). Every wrapper keeps its existing `#[cfg(feature = …)]` gates and platform-agnostic signature.
view_prefs.rs       — F1.3 per-library ViewPrefs/ViewFilters persistence for the landing page (sort/filter/facet state); web → localStorage keyed by library path, mobile → in-memory map (resets on cold launch), server (SSR) → defaults so first-hydration markup matches. Shape lives in omnibus-shared for a future server-backed endpoint
reader_progress.rs  — F2.2 reading-position **cache-aside layer** for the F2.1 progress-sync endpoint: web → localStorage keyed by uuid, mobile → in-memory map, SSR → no-op. The reader reads this sync for instant first-paint then reconciles against `GET /api/progress/{uuid}` (server wins on success), and writes here AND fire-and-forget POSTs to `/api/rpc/progress` on every page turn.
audiobook_progress.rs — F2.3 listening-position cache-aside layer, audio analogue of `reader_progress`. Stores seconds (float) keyed by uuid (`omn.listening::{uuid}`), plus per-book playback-rate preference (`omn.listening::rate::{uuid}`). Same web/mobile/SSR split; the listen page reads the local value for instant first-paint, reconciles against `GET /api/progress/{uuid}?format=audio`, and writes locally + fire-and-forget POSTs to `/api/rpc/progress` on every 5 s of playback plus on pause.
rpc.rs              — #[get]/#[post] server functions (mounted by dioxus::server::router); server bodies call into omnibus_db. F5b: `rpc_get_ebooks_page` (POST) is the keyset-paginated web landing read replacing the full-library `rpc_get_ebooks` — server-side sort (`SortKey`/`SortDir`) + `ViewFilters`, opaque `next_cursor`, and `total` + `facets` only on the cursor-less first page; returns shared `LibraryPage`. F3.1: `rpc_list_shelves`/`rpc_get_shelf`/`rpc_create_shelf`/`rpc_update_shelf`/`rpc_delete_shelf`/`rpc_get_shelf_page`/`rpc_add_shelf_books`/`rpc_remove_shelf_book`/`rpc_preview_shelf_rule` server functions (visibility enforced in-body via `db::can_view`/`can_edit`) back the shelves rail + detail + create modal.
pages/  — page components. `landing/`, `listen/`, `reader/`, `metadata_edit/`, `book_detail/` are directories with submodule splits (described below); the rest are flat `.rs` files (`settings`, `auth`, `author`, `authors_index`, `series`, `series_index`, `tag_cloud`, `search`, `shelf_detail`). auth.rs hosts LoginPage + RegisterPage. shelf_detail.rs is the F3.1 `/shelves/:id` page (rail + shelf header with ⋯ rename/visibility/delete; Smart = read-only rule chips + sort control + auto-sorted grid; Manual = description + member grid + "＋ Add books" picker; drag-reorder + post-create rule editing deferred). reader/ is the F2.4 immersive full-screen EPUB reader at /read/:uuid (no app nav; frosted-glass top/bottom chrome, Aa typography panel, circular page-turn buttons + ArrowLeft/ArrowRight keyboard nav, bottom bar with real page numbers via `book.locations.generate(1024)` + chapter data from `book.navigation.toc`, 2px progress ribbon; loads the vendored epub.js runtime, drives window.OmnibusReader via dioxus::document::eval, persists position via reader_progress; web-only interop, chrome compiles on every target). Split into submodules: `mod.rs` (thin BookReadPage/ReaderLayout chrome shell — top/viewer/page-turn chrome + panel wiring + the `reader_call` eval bridge), `signals.rs` (ReaderStatus/RelocateData state types, `format_progress_labels` bottom-bar formatter, `use_book_metadata` hook), `highlights.rs` (`spawn_create_highlight` optimistic create/rollback shared by the swatch/Note/Quote actions), `typography.rs` (F2.4b Typeface/LineSpacing/Margins enums + their `omn.*` localStorage persistence — functional typeface/line-spacing/margins/justify controls), `selection.rs` (F2.4b SelectionData payload from the glue's `selected` event + the SelectionPopover swatch picker), `aa_panel.rs` (ReaderAaPanel typography settings panel — theme/typeface/size/spacing/margins/justify + F2.4b **Page view** single/two-page via `Spread` pref → `rendition.spread`), `chrome_handlers.rs` (back/page-turn/Escape — Escape peels overlays before navigating back), `interop.rs` (web JS callbacks: relocate/status/selection/toc/search + `parse_file_id_from_url`), `prefs.rs`/`bootstrap.rs`. F2.4b interactive surface (Branch C/D): `selection.rs` popover gains Note/Copy/Quote/Share actions; `note_composer.rs` (anchored note editor → `update_highlight_note`); `toc_drawer.rs` (contents drawer from the glue's `__omnibusOnToc`); `highlights_drawer.rs` (color-filterable highlights & notes, navigate/copy/quote/delete); `search_panel.rs` (in-book search via glue `search` → `__omnibusOnSearchResults`); `quote_panel.rs` (quote card — live preview, bg presets + custom color, aspect ratios, Download PNG via the glue's canvas-drawing `exportQuoteCard`); `reader_bookmarks.rs` (reader bookmarks on the unified backend, `position` = CFI). New top-chrome buttons: Contents/Search/Highlights(+count badge)/Bookmark. F2.4b highlights round-trip through `data::*_highlight` → `/api/highlights/*` (loaded on book open, re-applied as epub.js annotations) and now persist the selected `text`. CSS lives in `atrium.css` under `.rd-*` classes. Vendored reader runtime lives at frontend/assets/vendor/{jszip.min.js,epub.min.js,epub-reader-glue.js} and is loaded only on the reader page. listen.rs is the F2.3 immersive audiobook player at /listen/:uuid — plain HTML5 `<audio>` element (no vendored library), inline `dioxus::document::eval` bootstrap exposes `window.OmnibusAudio` (play/pause/seek/skip/setRate plus `initDirect`/`initHls`). Boots by fetching `/api/audiobooks/:uuid/manifest` and branching: `mode:"direct"` chains Range-served per-part URLs (m4b/m4a/mp3, instant) with `ended`-driven advance; `mode:"hls"` falls back to status-polling + hls.js attach. Surfaces `state:"failed"` from the /status endpoint as a real failure overlay (Bug 4 of #338). Persists position via audiobook_progress + the F2.1 endpoint. F2.3b: playback state (book/uuid/elapsed/duration/playing/rate/chapters/hls_ready/playback_failed) is app-global via the `PlaybackState` context — the page sets `playback.uuid` on mount and renders from context; the App-level `install_audio_bootstrap` driver does the fetch + manifest init. Split into submodules: `bootstrap.rs` (web JS interop + `install_audio_bootstrap` driver, called once from App), `controls.rs` (AudioElement — now mounted at the App root, not per-route — plus Scrubber/TransportButtons/Toolbar), `stage.rs` (two-column layout + `kicker_text`/`chapter_sub_text` helpers), `mini_dock.rs` (F2.3b persistent bottom dock — cover/title/chapter/progress/±30s/play-pause/rate/Expand/dismiss; rendered by `ScreenLayout`, reuses `audio_call` + `chapter_index_for_elapsed` + `chapter_sub_text`; `.mini-dock*` CSS), `chapter_map.rs` (segmented chapter progress bar replacing the range scrubber — flex segments sized by duration, accent fill for played portions, click-to-seek), `speed_panel.rs` (frosted-glass speed overlay with preset grid + fine-tune slider), `sleep_panel.rs` (sleep-timer overlay) + `sleep.rs` (F2.3b PR5 countdown engine: presets + end-of-chapter, live toolbar label, pause + 30s volume fade on expiry via `OmnibusAudio.setVolume`), `chapters_drawer.rs` (right-side chapters drawer with played/current/upcoming row states, click-to-seek), `bookmarks_drawer.rs` + `bookmarks.rs` (F2.3b PR4 bookmarks on the unified backend: list/create/inline-note/delete + "Bookmark saved" toast), `overlays.rs` (failed/preparing states), `helpers.rs` (format_hms, audio_call), `ready_player.rs` (post-load orchestrator; owns chapter tracking via use_memo + chapter skip handlers; mounts the app-wide TopNav instead of a custom bar). CSS lives in `atrium.css` under `.lp-*` classes. Landing is the primary Atrium consumer (cover grid + power-user table) and the source of format-faceted filtering (ViewFilters.formats in shared). F5b: **browse** (no search query) is keyset-paginated server-side via `data::get_ebooks_page` → `rpc_get_ebooks_page` (server owns sort + filter; facets + total ride the first page; further pages append from a `lib-load-more` sentinel, auto-clicked on web by an `IntersectionObserver`); **search** keeps the pre-F5b path (capped result set sorted/filtered client-side via `sorting.rs`/`filtering.rs`). Changing sort/dir/filters refetches page 1, while view-mode/sidebar toggles don't (a `use_memo` keys the fetch effect on only the data-affecting prefs). metadata_edit.rs is the F5.1 single-book edit form at /books/:id/edit. book_detail/ is the Atrium "Cinematic" `/books/:uuid` detail page, split into submodules: `mod.rs` (BookDetailPage shell — book/author-books/F3.3-suggestions fetch effects with an epoch guard, admin gating, shared markup primitives BdCrumb/BdSectionHead/BdFormatBadge/BdMetaRow/BdInsightCell, `derive_loaded_view` → `render_loaded` composition), `hero.rs` (BdHeroSection — breadcrumb, cover + format badges, title/CTAs Start-reading/Listen, tag chips, rating card), `body.rs` (BdBodyMain two-column: public journal feed + highlights stub + "From the same hand" author fan + `BdSuggestionsStrip` (F3.3 Ready/Pending/NotConfigured states + admin connect card) — plus the sticky BdRailSection of file details/`FormatSwitcher`/series/insights), `rating.rs` (BdRatingWidget — interactive 0.5–5.0 half-star widget, post-mount load + reconcile, click-active-to-unrate), `journal.rs` (BdJournalSection — F3.2 public reading-journal feed + owner-only composer, delegated spoiler reveal), `journal_editor.rs` (web-only live-markdown editor glue shipping the co-located `journal_editor.js` over the eval channel; SSR/native fall back to a plain textarea), `merge.rs` (web-only F5.10 `build_merge_button`/`build_merge_ui` for the admin "Merge with…" dialog + post-merge toast; mobile compiles to `None`-returning stubs). Discovery pages (author, series, tag_cloud) are F1.8. authors_index.rs and series_index.rs are the F1.12 `/authors` and `/series` browse-all index surfaces. search.rs is the full-page /search/:query results view that the F1.5 palette routes into when the user presses Enter without arrow-key navigation, or when a Tag row is selected. F6.1 mobile designs: the native shell renders dedicated single-column layouts via `#[cfg(feature = "mobile")]` presentation branches that reuse each page's shared data-fetch shell — `landing/mobile.rs` (`MobileLanding`: "Your shelf" header + Shelves entry + 3-col `Cover` grid + Load-more), `book_detail/mobile.rs` (`render_loaded_mobile`: accent-radial hero + About/rating/info/files/journal sections; the web `hero.rs`/`body.rs` cluster is `not(mobile)`), and a centered `auth.rs` mobile branch (`m_auth_shell`); `pages/shelves_index.rs` is the new `/shelves` list (`Route::Shelves`, reached from the mobile library header, styled with `.m-shelf-row`), while `shelf_detail.rs` reuses its shared markup styled for phone widths. All mobile styling lives under `.m-*` classes in atrium.css; the mobile build never compiles the web landing table/toolbar cluster.
components/{top_nav,bottom_nav}.rs  — feature = web / mobile respectively. bottom_nav.rs (F6.1) is the mobile 4-tab icon bar (Library→Landing, Authors→AuthorsIndex, Series→SeriesIndex, You→Settings); `is_active` lights a tab across its whole section. `.m-tabbar*` CSS.
components/atrium.rs  — F1.7 design-system primitives (AtriumRoot, Cover); the app-wide `Theme` enum (Dark/Light/Sepia) + init_theme/persist_theme; CSS at frontend/assets/atrium.css. Live theme switcher is UmThemeSeg in user_menu.rs.
components/search_palette/ — F1.5 command-palette search overlay (⌘K trigger, grouped FTS5 results, keyboard nav); web-only (not(mobile)). Split into `{mod,keyboard,model,overlay,results}.rs` (mod = overlay shell, keyboard = nav, model = state, overlay = render, results = grouped FTS rows)
components/format_switcher.rs — F1.4 per-format CTA rows on the book detail page (Read for EPUB → F2.2 reader; Listen for M4B/M4A/MP3 → F2.3 player at /listen/:uuid on web, disabled on mobile; Send-to-Kindle still stubbed); the UI contract for the F0.1 books/book_files split
components/merge_dialog.rs — F5.10 admin "Merge with…" dialog mounted by the book detail page (web-only): debounced candidate search via POST /api/rpc/merge-books/candidates (FTS across BOTH libraries — rpc_search is ebook-scoped and would never surface audiobooks) → confirm → POST /api/rpc/merge-books; the page's book is always the merge target (target metadata wins). Parent renders the undo toast (POST /api/rpc/merge-books/undo).
components/author_photo_edit.rs — F1.11 admin photo-edit overlay (web-only): hover-revealed pencil button over the avatar opening a modal with paste-URL / file-upload (→ PUT /api/authors/:id/photo) / re-scan-Open-Library actions. Wraps avatars on the author detail and authors-index pages
components/chip_editor.rs — F5.9-lite reusable add/remove chip editor with ≤5-row prefix-match suggestion dropdown (↓/↑ navigates, Enter commits, Esc clears). Renders chips + input + dropdown as siblings of the parent flex row so the existing `me-chip-row` / `me-tag-chips` layouts keep their flow. Consumed by the F5.1 metadata edit page (authors + tags) — the consumer passes a `Vec<String>` candidate pool (typically derived from `data::list_authors` + `data::get_tag_cloud`); empty pool degrades to plain free-text entry.
components/shelves_rail.rs — F3.1 left rail on the Library + shelf-detail pages (`RailActive::{All, Shelf(id)}`): SSR-safe shelf fetch via `data::list_shelves`, "All books" home row + per-shelf rows (cog marker for Smart / accent swatch for Manual, count, lock/people visibility icon), "＋ New shelf" button opening a locally-owned `CreateShelfModal`. Replaced the old landing filter sidebar.
components/create_shelf_modal.rs — F3.1 create modal: name + Smart/Hand-picked toggle + Private/Public control. Smart body = match-any/all + field-aware condition rows (field/op selects gated by `RuleField::accepts`, value inputs aware of rating/year/date/InLast-unit/Between) + live `data::preview_shelf_rule` "N of M match" preview. Hand-picked body = searchable selectable cover grid. Errors surface in a `role="alert"`.
components/auth/{mod,shell,banner,field,strength}.rs — F1.6 auth-UI primitives (purely presentational, SSR/WASM identical for hydration) used by pages/auth.rs: AuthShell split-pane wrapper, Banner callout (err/warn/info/ok), Field label+input+hint/error/success slots, StrengthMeter four-segment password bar
components/user_menu.rs — avatar trigger + dropdown panel in TopNav; hosts the real Settings link, Sign out action, and Dark/Light theme segmented control (Sepia stubbed). Most rows are forward-looking stubs (disabled <a>) until the underlying features (Profile, Journal, Highlights, Goals, Notifications, Switch user) ship. Web-only (not(mobile)).
```

## server/src/

```
main.rs             — dioxus::launch (WASM) / dioxus::serve (native); mounts auth_router + rate-limit + origin-check + security_headers
lib.rs              — re-exports backend + auth + rate_limit + security_headers under `server` feature for tests
backend.rs          — /api/* REST router (mobile-facing) + integration tests; `/api/search/*` sub-router carries its own per-IP rate-limit layer. F5b: `GET /api/ebooks` takes optional `?sort=&dir=&cursor=&limit=` for a keyset page (emits `X-Next-Cursor`, 400 on a malformed cursor); with **no** params it returns the full capped library byte-identically to before, so existing mobile clients are unaffected (REST keyset is sort+cursor only — sidebar filters are a web concern). `GET /api/ebooks/{uuid}/file` (F2.2) streams the raw EPUB bytes (application/epub+zip, cookie-gated) for the reader; resolves the on-disk path via `db::book_file_path`. F2.1 sub-module `backend/progress.rs` mounts `POST /api/progress` (discriminated `{format: "epub"|"audio"}` payload, last-write-wins, 404 on unknown book), `GET /api/progress/{uuid}?format=…`, and `POST /api/progress/sessions` (batched `SessionReport` rows). F2.4b sub-module `backend/highlights.rs` mounts `POST /api/highlights`, `GET /api/highlights/book/{book_uuid}`, `PATCH /api/highlights/{id}/color`, `PATCH /api/highlights/{id}/note`, `DELETE /api/highlights/{id}`. F2.3b/F2.4b sub-module `backend/bookmarks.rs` mounts `POST /api/bookmarks`, `GET /api/bookmarks/book/{book_uuid}`, `PUT /api/bookmarks/{id}`, `DELETE /api/bookmarks/{id}`. F3.2 sub-module `backend/ratings.rs` mounts `POST /api/ratings` (0.5–5.0 half-star steps, last-write-wins, 400 on out-of-range/non-half-step, 404 unknown book), `GET /api/ratings/{uuid}`, `DELETE /api/ratings/{uuid}` (un-rate). F3.2 sub-module `backend/journals.rs` mounts the **public** journal feed: `POST /api/journals` (400 empty/oversized body, 404 unknown book), `GET /api/journals/book/{book_uuid}` (every user's entries), `POST /api/journals/preview` (markdown→sanitized HTML), `PATCH`/`DELETE /api/journals/{id}` (owner-scoped, 404 for a non-owner). F3.3 sub-module `backend/suggestions.rs` mounts `GET /api/ebooks/{uuid}/suggestions` (drives the `db::decide` cache state machine: `NotConfigured` with no key, else marks `pending` + enqueues one `Task::ResolveSuggestions` when the cache is absent/stale, serving fresh/sticky rows), `GET /api/suggestions/{uuid}/{rank}/cover` (streams a cached cover, 404 on miss), and the admin-only `GET`/`POST /api/hardcover-key` (masked read / save-or-clear, 422 on an over-length key). F3.1 sub-module `backend/shelves.rs` mounts `GET`/`POST /api/shelves`, `POST /api/shelves/preview`, `GET`/`PATCH`/`DELETE /api/shelves/{id}`, `GET /api/shelves/{id}/page?sort=&dir=`, `POST /api/shelves/{id}/books`, `DELETE /api/shelves/{id}/books/{uuid}` — per-request visibility (owner∨admin∨public view; owner∨admin edit → 403/404), 409 on duplicate name, 422 on invalid rule.
backend/audiobooks.rs — F2.3 audiobook playback routes: `GET /api/audiobooks/{uuid}/manifest` returns `{mode:"direct", parts:[…]}` (m4b/m4a/mp3 — instant playback) or `{mode:"hls", playlist_url}` (mixed folders containing flac/ac3/… — falls back to transcode) per `db::audiobook::codec::classify_filenames`. `GET /api/audiobooks/{uuid}/parts/{ordinal}` Range-serves source files via `tower_http::ServeFile`; mirrors the same classifier gate so HLS-classified books 404 instead of leaking source bytes around the manifest contract. Legacy HLS routes (`/playlist.m3u8`, `/segments/{seg}`, `/status`) retained as the fallback path. See #339.
backend/uploads.rs  — "add your own books" REST: `POST /api/uploads/ebooks/inspect` (parse an uploaded EPUB → UploadInspection) and `POST /api/uploads/ebooks` (file into the canonical folder via `library_layout::allocate_canonical_path`, `Task::Scan` + `await_completion` so the indexer inserts the book, then `merge_metadata_overrides` for any user edits). Gated on `can_upload`/admin. Audiobook routes stubbed 501. Mounted in a `book_upload_router` carrying its own `OMNIBUS_MAX_UPLOAD_BYTES` body limit + the upload rate limiter.
rate_limit.rs       — reusable in-memory per-IP fixed-window counter + `rate_limit_by_ip` axum middleware; `rate_limit_paths` is a path-prefix wrapper used for both `/api/rpc/search-palette` and the auth router (allow-list = login/register/logout, so `/api/auth/me` is deliberately exempt from the 10/60s bucket)
security_headers.rs — #277 global HTTP security response headers: CSP (Dioxus-WASM-compatible), X-Frame-Options DENY, Referrer-Policy strict-origin-when-cross-origin, X-Content-Type-Options nosniff applied unconditionally; HSTS (1y + includeSubDomains) gated on OMNIBUS_SECURE_COOKIES so plain-HTTP dev origins don't advertise the wrong policy
auth/mod.rs         — /api/auth/{register,login,logout,me} + AuthUser/AdminUser/MediaAuthUser extractors + CSRF origin-check. MediaAuthUser gates the image-read endpoints (covers/thumbs/author-photo/suggestion-cover) and additionally accepts the session as a `?token=` query param — the only auth a mobile WebView `<img>` fetch can carry (bearer header + cookie are both absent on those requests)
auth/gate.rs        — top-level middleware gating /api/* (pass-through for /api/auth/*). GET requests to media read paths (`is_media_read_path`) may authenticate via `?token=` so the boundary gate doesn't 401 mobile `<img>` fetches before MediaAuthUser runs
auth/strategy.rs    — AuthStrategy trait + PasswordStrategy (OIDC/WebAuthn fit the same shape)
auth/boot.rs        — OMNIBUS_INITIAL_ADMIN recovery hook (promotes named user to admin)
```

## ui_tests/playwright/

```
tests/
  flows/            — one *.spec.ts per user flow
  utils/            — cross-flow helpers (nav, api mutation assertions)
  fixtures/         — extended `test` / `expect` exports
```

## mobile/src/

```
main.rs             — dioxus::launch, ServerUrl context, hydrates bearer token from disk, wraps omnibus_frontend::App
```

Mobile auth: bearer-token login flow lives in `frontend/src/data.rs` under
`feature = "mobile"`. `data::mobile_login` / `mobile_register` POST to
`/api/auth/{login,register}` with `client_kind: ios|android|bearer` so the
server returns a bearer token in the JSON body. `data::token_store` keeps
the token in a process-local `OnceLock<RwLock<...>>` and — **only in
debug builds** (`cfg!(debug_assertions)`) — persists it to
`$HOME/.omnibus-token`. Release builds keep the token in memory only and
require re-login on every cold start. UI components subscribe to
`data::token_store::subscribe()` (a `tokio::sync::watch` receiver) so a
401-driven `token_store::clear()` reactively redirects to `/login`.
**TODO**: replace the disk-persistence stub with iOS Keychain / Android
Keystore and flip persistence on unconditionally — see the module docs.
